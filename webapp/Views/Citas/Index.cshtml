@model IEnumerable<Falco.WebApi.Models.Cita>

@{
    ViewBag.Title = "Citas";
}

<div id="content">

    <div class="row">
        <div class="col-xs-12 col-sm-7 col-md-7 col-lg-4">
            <h1 class="page-title txt-color-blueDark">
                <i class="fa fa-pencil-square-o fa-fw "></i>
                Citas
            </h1>
        </div>
        <div class="clearfix"></div>
        <div class="col-sm-5">
            <p>
                @Html.ActionLink("Crear Nuevo", "Create")
            </p>
            <div class="table-responsive">
                <table class="table table-bordered">
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.tipoCita.Descripcion)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.PacientId)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Fecha)
                        </th>
                        <th></th>
                    </tr>

                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.tipoCita.Descripcion)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.PacientId)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Fecha)
                            </td>
                            <td>
                                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                            </td>
                        </tr>
                    }

                </table>
            </div>
        </div>

    </div>
</div>


